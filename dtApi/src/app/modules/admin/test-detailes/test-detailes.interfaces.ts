export interface TestDetails {
    test_id: number;
    id: number;
    level: string;
    tasks: string;
    rate: string;
    response?: string;
}
export interface TestRate {
    testRate: string;
}
